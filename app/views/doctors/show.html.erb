<h1><%= @doctor.name %></h1>

Dept: <%= @doctor.department %>
<br>
Appointments: 
<br>
<% @doctor.appointments.each do |appt| %>
    <br>
    <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> with <%= link_to appt.patient.name, patient_path(appt.patient) %>
<% end %>
