<h1>Doctors</h1>

  <ul>
    <% @doctors.each do |doctor| %>
      <li>
        Doctors name: <%= doctor.name  %>
        Link to page: <%= link_to doctor.name, doctor_path(doctor)  %>
        Patients: <%= doctor.patient.count  %>
      </li>
    <% end %>
  </ul>