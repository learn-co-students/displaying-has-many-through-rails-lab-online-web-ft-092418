<h1><%= @patient.name %></h1>

<br>
Appointments: 
<br>
<% @patient.appointments.each do |appt| %>
    <br>
    <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> with <%= link_to appt.doctor.name, doctor_path(appt.doctor) %>
<% end %>
