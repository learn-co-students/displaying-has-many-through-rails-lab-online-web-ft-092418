<h1><%= @patient.name %></h1>

<ul>
  <% @patient.appointments.each do |appointment| %>
    <li> <%= link_to(appointment.doctor.name, doctor_path(appointment.doctor)) %> - <%= appointment.to_stringy %></li>
  <% end %>
</ul>
